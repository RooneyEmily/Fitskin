# Chart CC JPEG cohort (FitSkin Phase 4)

6 trials × 2 frames (no-flash + flash) = 12 JPEGs with **ColorChecker visible** in scene.

Used by `run_chart_cc.py` (in-scene chart correction → cheek Lab vs FitSkin).

Not the same as `data/phase4_booth_raw/` (booth DNGs for flash/no-flash Pipeline 4).
